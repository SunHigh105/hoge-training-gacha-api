interface TrainingPattern {
  muscle: Array<Number>,
  cardio: Array<Number>
}

interface Training {
  category: string,
  channelId: string,
  minute: string,
  thumbnail: string,
  title: string,
  url: string,
  videoId: string,
}
